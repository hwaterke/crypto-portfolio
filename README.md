# Setup

Create a file called `creds.json` in the `src` folder.

It should look like this

```json
{
  "KEY": "xxx",
  "B64_SECRET": "xxx",
  "PASSPHRASE": "xxx",
  "API_URI": "https://api.gdax.com"
}
```
